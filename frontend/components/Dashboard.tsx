'use client'
import {useEffect,useMemo,useState} from 'react'
import Link from 'next/link'
import {getJSON} from './api'
export default function Dashboard(){const [data,setData]=useState<any[]>([]);useEffect(()=>{getJSON('/datasets').then(setData).catch(()=>{})},[]);const m=useMemo(()=>({rows:data.reduce((a,d)=>a+d.rows,0),gb:data.reduce((a,d)=>a+d.size_gb,0),cold:data.filter(d=>d.eligible).reduce((a,d)=>a+d.size_gb,0),candidates:data.filter(d=>d.eligible).length}),[data]);return <><div className="top"><div><div className="eyebrow">Governed data tiering</div><h1 className="h1">Data estate temperature</h1><div className="muted">Evidence-backed archival recommendations, safe execution, verified restoration.</div></div><div className="live">● DataHub context connected</div></div><div className="grid"><div className="card"><div className="metric-label">Hot estate</div><div className="metric">{m.gb.toFixed(1)} GB</div></div><div className="card"><div className="metric-label">Cold candidates</div><div className="metric">{m.cold.toFixed(1)} GB</div></div><div className="card"><div className="metric-label">Candidate datasets</div><div className="metric">{m.candidates}</div></div><div className="card"><div className="metric-label">Observed rows</div><div className="metric">{m.rows.toLocaleString()}</div></div></div><div className="section card"><h2>Temperature map</h2><table className="table"><thead><tr><th>Dataset</th><th>Temperature</th><th>Class</th><th>Risk evidence</th><th>Decision</th></tr></thead><tbody>{data.map(d=><tr key={d.urn}><td><b>{d.name}</b><div className="muted">{d.domain} · {d.owner}</div></td><td><div style={{display:'flex',alignItems:'center',gap:10}}><div className="heatbar"><div className="heatfill" style={{width:`${Math.max(d.temperature,2)}%`}}/></div>{d.temperature}°</div></td><td><span className={'pill '+d.classification.toLowerCase()}>{d.classification}</span></td><td>{d.evidence?.[2]?.label||'Context analyzed'}</td><td><Link className={d.eligible?'eligible':'blocked'} href={`/candidates?id=${d.urn.split(',').pop()?.replace(')','')||''}`}>{d.eligible?'Review candidate':'Keep hot'}</Link></td></tr>)}</tbody></table></div></>}
