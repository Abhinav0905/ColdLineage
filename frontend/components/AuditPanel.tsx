'use client'
import {useEffect,useState} from 'react';import {getJSON} from './api'
export default function AuditPanel(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{getJSON('/audit').then(setItems)},[]);return <><div className="top"><div><div className="eyebrow">Provenance</div><h1 className="h1">Audit trail</h1><div className="muted">Plans, simulations, approvals, archives, restores and DataHub writeback are retained as evidence.</div></div></div><div className="section card"><table className="table"><thead><tr><th>Event</th><th>Dataset</th><th>Actor</th><th>Time</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.event_type}</td><td>{x.dataset_urn}</td><td>{x.actor}</td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></>}
