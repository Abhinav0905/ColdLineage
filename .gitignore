# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Environment
.env
.env.local
.env.*.local
!.env.example

# DataHub CLI local state
.datahubenv

# OS
.DS_Store
Thumbs.db

# Local scratch — never commit run output; examples/ is curated by hand
scratch/
*.log
.venv-agent/

# Internal component library, vendored locally for evaluation only.
# NOT part of this submission: it is AiDash-internal source, and origami/.npmrc
# carries an auth token for the private verdaccio registry. Publishing either to a
# public repo would leak company IP and a live credential.
origami/
